# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /coinbase/products                       controllers.CoinbaseController.products
GET     /coinbase/candles                        controllers.CoinbaseController.candles(pairId: String, start: String, end: String, granularity: String)

